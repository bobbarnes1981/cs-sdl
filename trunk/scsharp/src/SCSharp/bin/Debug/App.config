<configuration>
  <configSections>
  </configSections>
  <appSettings>
    <!-- this should be the path to a directory containing the
	 StarDat.mpq and BrooDat.mpq files -->
    <add key="StarcraftDirectory" value="C:\\data\\SCSharpData" />

    <!-- this should be the path to a directory containing the
	 install.exe file from the Brood Wars CD -->
    <add key="BroodwarCDDirectory" value="C:\\data\\SCSharpData\\BW" />

    <!-- this should be the path to a directory containing the
	 install.exe file from the Starcraft CD -->
    <add key="StarcraftCDDirectory" value="C:\\data\\SCSharpData\\SC" />

    <!-- debugging/development options.

         ShowElementBorders:
              puts a border around UI elements in the screens leading
              up to the game screen.  A green border means the element
              is visible, yellow means not visible.

         ShowSpriteBorders:
              puts a green border around a sprite's bounding box in
              the game screen.
    -->

    <!--<add key="ShowElementBorders" value="true" />-->
    <!--<add key="ShowSpriteBorders" value="true" />-->
  </appSettings>
</configuration>
